# Rust build artifacts (headwater/, usgs-manifest/, streamview/)
target/

# streamview: trunk's build output (wasm bundle, JS glue, copied assets)
dist/

# Data: reproducible from the USGS API / from re-running the pipeline, and
# large/binary-heavy in a way git doesn't handle well. Remove these two
# lines if you'd rather have the lake and published datasets tracked in git.
headwater/maumee_lake/
streamview/published/

# Local env/secrets (e.g. USGS_API_KEY), if you ever add one
.env
.env.local

# macOS
.DS_Store

# Editors
.vscode/
.idea/
*.swp
